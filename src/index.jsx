// import render method
import { render } from 'react-dom';

// create root const
const root = document.getElementById('root');

// render the app
render(
  <React.StrictMode>
    <h1>Hey</h1>
  </React.StrictMode>,
  root
);
